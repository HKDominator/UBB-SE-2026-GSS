<Page
    x:Class="Events_GSS.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <NavigationView
        x:Name="NavView"
        IsBackButtonVisible="Auto"
        IsBackEnabled="{x:Bind CanGoBack, Mode=OneWay}"
        ItemInvoked="OnNavItemInvoked"
        BackRequested="OnBackRequested"
        PaneDisplayMode="Left"
        OpenPaneLength="200">

        <NavigationView.MenuItems>
            <NavigationViewItem
                Content="Events"
                Tag="EventListing">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE716;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <NavigationViewItem
                Content="My Events"
                Tag="MyEvents">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE787;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <NavigationViewItem
                Content="Notifications"
                Tag="Notifications">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE787;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>

            <!-- Statistics navigation removed from global menu. Statistics are per-event and available
                 from the Event details page for the event admin only. -->
        </NavigationView.MenuItems>



        <Frame x:Name="ContentFrame" />

    </NavigationView>
</Page>